'use client';

import { Target } from 'lucide-react';

import { PlaceholderPage } from '@/components/ui/PlaceholderPage';

export default function MetasPage() {
  return (
    <PlaceholderPage
      title="Metas"
      icon={Target}
      phase="Fase 5"
      description="Objetivos com progresso visual, ligação a tarefas e widgets no dashboard."
    />
  );
}
